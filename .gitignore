# Python / venv
.venv/
__pycache__/
*.pyc

# Secrets — NEVER commit. Auth is via `databricks auth login` (OAuth, stored in
# ~/.databrickscfg outside the repo). Put any PAT/token/password overrides here:
.env.local
*.pem
*.key

# Generated per-environment state (regenerated by the deploy scripts)
deploy/resolved.json
app/config.json

# OS / editor
.DS_Store
